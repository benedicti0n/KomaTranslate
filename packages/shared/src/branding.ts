/** Public-facing extension name and identifiers. */
export const EXTENSION_NAME = 'Manga Translator';
export const EXTENSION_SHORT_NAME = 'MangaTL';

/** Internal namespace used for storage keys and logging. */
export const NAMESPACE = 'mangaTranslator';

/** Storage key for the set of sites enabled by the user. */
export const ENABLED_SITES_STORAGE_KEY = `${NAMESPACE}:enabledSites`;

/** Storage key for extension settings. */
export const SETTINGS_STORAGE_KEY = `${NAMESPACE}:settings`;

/** Logging prefix for console output. */
export const LOG_PREFIX = '[Manga Translator]';
